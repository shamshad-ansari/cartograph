# macOS
.DS_Store

# CMake build tree
/build/

# Editor tooling index (symlink into build/)
/compile_commands.json

# Editor/IDE settings
/.vscode/

# Persisted index cache written by `cartograph index` (ADR-0008)
.cartograph.idx

# Local issue tracker (not versioned)
/issues/